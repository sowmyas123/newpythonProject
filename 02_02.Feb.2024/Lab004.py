print("Sowmya")
print("sowmya", "BS", end="$")
print("sowmya", "BS", "Bangalore", sep="_")
#click on print to see builtins
#def print(self, *args, sep=' ', end='\n', file=None): # known special case of print
  #Prints the values to a stream, or to sys.stdout by default.
  #*args - any no of arguments
  #sep - space
  #end - new line
  #* - wild character i.e any no of characters, string, arguments
  #argument - is what we put into the function
      #sep
       # string inserted between values, default a space.
      #end
       # string appended after the last value, default a newline.
      #file
       # a file-like object (stream); defaults to the current sys.stdout.
      #flush
     #   whether to forcibly flush the stream.
#print function can also take multiple arguments

result = max(3,4)
print(result)

#to see builtin functions google prebuilt python documentation